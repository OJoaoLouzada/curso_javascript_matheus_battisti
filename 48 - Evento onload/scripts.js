// Evento onload serve para executar o evento após o carregamento do DOM
// O evento onload permite carregar os scripts antes do HTML (na head)

window.onload = function(){
    console.log('Carregou o DOM');
}
console.log('Carregou o JS');